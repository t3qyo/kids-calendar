@AGENTS.md

# Kids Calendar Maker

プロジェクトの基本情報・セットアップ・デプロイフローは `README.md` を参照してください。
ここでは Claude Code に守ってほしい行動指針のみ記載します。

## 守ってほしいこと

### ブランチ運用
- `main` への直接 push は禁止。基本フロー: `feature/*` → `develop` → `main`
- 新機能や修正を始める前に、起点となるブランチを確認する（基本は `develop` 起点）
- main にマージするのは「明示的に指示があったとき」のみ

### ツール
- パッケージマネージャは **yarn**（`npm install` 等は使わない）
- 開発ポートは **3010**（`yarn dev` で起動）
- 変更後は `yarn build` でビルドが通ることを確認してから報告

### 写真・手書き数字の処理
- すべてクライアントサイドで完結させる方針（サーバー送信しない）
- プライバシー観点で重要。外部API・サーバー処理を使う実装はユーザーに確認してから進める

### 商用利用に関する注意
- 現在は Vercel Hobby プランで運用中
- AdSense・課金・アフィリエイト等の商用要素を入れる話が出たら、Hobby 規約に触れる点を先に伝える（Pro 移行 or Cloudflare Pages 等への移行が必要）
